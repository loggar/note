{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch via babel-node",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["babel-node", "--presets", "@babel/preset-env"],
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
