# WSL Distributions

## List Distros

For Windows 10 version 1903 and later:

```
wsl --list --all

wsl -l -all

wsl -l -v
```

For versions earlier than Windows 10 version 1903:

```
wslconfig /list /all

wslconfig /l /all
```
