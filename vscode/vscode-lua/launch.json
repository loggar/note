{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Lua",
      "type": "lua",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}